import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // set temporary redirect for dashboard
  // async redirects() {
  //   return [
  //     { source: '/', destination: '/', permanent: false },
  //   ]
  // },
};

export default nextConfig;
